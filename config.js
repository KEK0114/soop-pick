// ─────────────────────────────────────────────────────────────────────────
//  관리자가 만지는 곳. 여기만 고치면 화면 문구·색·기본값이 바뀝니다.
//  index.html 은 열지 않아도 됩니다.
//
//  고친 뒤:  git add . && git commit -m "문구 수정" && git push
//            → Vercel 이 알아서 다시 올립니다 (1분 안쪽).
// ─────────────────────────────────────────────────────────────────────────
window.PICK_CONFIG = {

  // ── 이름과 설명 ────────────────────────────────────────────────────────
  title: '좋아요 순위',
  subtitle: 'SOOP 방송국 게시글에 달린 <b>댓글을 좋아요 순으로</b> 줄 세웁니다. ' +
            '뽑을 인원을 넣으면 커트라인을 그어 줍니다.',

  // 주소창 · 검색결과에 뜨는 이름
  pageTitle: '좋아요 순위 — SOOP 게시글 지원자 줄 세우기',

  // ── 기본값 ────────────────────────────────────────────────────────────
  defaultPickCount: 20,        // '뽑을 인원' 처음 값
  defaultPostUrl: '',          // 늘 같은 글을 본다면 여기에 주소를 넣어 두세요

  // ── 색 ────────────────────────────────────────────────────────────────
  // 바꾸고 싶은 것만 두면 됩니다. 지우면 기본색을 씁니다.
  colors: {
    // bg:     '#0e1116',      // 배경
    // panel:  '#161b23',      // 카드
    // accent: '#4c8dff',      // 강조 (버튼)
    // like:   '#ff6b8a',      // 좋아요 숫자
    // ok:     '#3fb950',      // 커트라인 안쪽
  },

  // ── 화면에 보일 것 고르기 ──────────────────────────────────────────────
  show: {
    followers: true,           // 팔로워 수 (순위에는 안 씁니다)
    // 스트리머가 지원서를 **읽었다는 표시**(SOOP 의 하트). 좋다는 뜻이 아니라
    // 확인했다는 뜻이라, 확인한 쪽이 아니라 **아직 안 본 쪽**에 표를 답니다.
    checked: true,
    bestBadge: true,           // 베스트 댓글 표시
    photos: true,              // 댓글에 붙은 이미지
    search: true,              // 찾기 칸
    excel: true,               // [엑셀로 내려받기] 버튼
    copyButton: true,          // 뽑힌 사람 복사
  },

  // ── 맨 아래 안내문 ────────────────────────────────────────────────────
  footer: [
    '좋아요 수는 <b>SOOP 에서 그때그때 직접</b> 가져옵니다. 저장하지 않으니 새로고침하면 늘 최신입니다.',
    '순위는 <b>좋아요 순서 하나</b>로만 매깁니다. 팔로워 수는 참고용으로 보여 줄 뿐 순위에 넣지 않습니다.',
    '한 사람이 댓글을 여러 개 달았으면 <b>좋아요가 가장 많은 것</b> 하나만 셉니다. 답글(대댓글)은 지원으로 세지 않습니다.',
  ],

  // ── SOOP 주소 ─────────────────────────────────────────────────────────
  // 지금은 보는 사람 브라우저가 SOOP 를 직접 부릅니다(CORS 가 열려 있음).
  // 나중에 막히면 여기를 중계 서버 주소로 바꾸면 됩니다. 예:
  //   apiBase: 'https://soopchat.duckdns.org/pick-proxy'
  apiBase: null,
};
